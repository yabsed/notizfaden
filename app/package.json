{
  "name": "notizfaden",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm --workspace client run dev",
    "build": "npm --workspace client run build",
    "check": "npm --workspace client run check",
    "test": "npm --workspace client run test",
    "server": "bash scripts/server.sh",
    "db": "bash scripts/db.sh",
    "start": "bash scripts/dev.sh",
    "android:sync": "npm --workspace client run android:sync",
    "android:build": "bash scripts/android.sh"
  }
}
